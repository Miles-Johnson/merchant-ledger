# Pricing Source Policy (Active)

Authoritative pricing source:

- `data/raw/lr_economy_workbook.xlsx`

All legacy pricing CSV artifacts are deprecated and moved under:

- `data/raw/deprecated_prices_20260416/`

Notes:

- `industrial_goods.csv` at `data/raw/industrial_goods.csv` is a legacy file retained only because it is currently locked by another local process.
- It has been copied to the deprecated folder and should be removed once the lock is released.
